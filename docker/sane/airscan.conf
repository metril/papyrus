# sane-airscan configuration
# Device entries are managed automatically by Papyrus (scanner probe + startup restore).
# Manual example for Brother WSD scanner:
#
# [devices]
# "Brother DCP-L2540DW" = http://192.168.1.100:80/WebServices/ScannerService, wsd

[options]
discovery = enable
