services:
  papyrus:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    network_mode: host
    volumes:
      - papyrus_data:/app/data
      - cups_spool:/var/spool/cups
    environment:
      - PAPYRUS_DB_URL=postgresql+asyncpg://papyrus:${POSTGRES_PASSWORD:-secret}@localhost:5432/papyrus
      - PAPYRUS_OIDC_ISSUER=${PAPYRUS_OIDC_ISSUER}
      - PAPYRUS_OIDC_CLIENT_ID=${PAPYRUS_OIDC_CLIENT_ID}
      - PAPYRUS_OIDC_CLIENT_SECRET=${PAPYRUS_OIDC_CLIENT_SECRET}
      - PAPYRUS_PRINTER_URI=${PAPYRUS_PRINTER_URI:-ipp://192.168.1.100/ipp}
      - PAPYRUS_PRINTER_NAME=${PAPYRUS_PRINTER_NAME:-Brother_DCP_L2540DW}
      - PAPYRUS_NETWORK_PRINTER_NAME=${PAPYRUS_NETWORK_PRINTER_NAME:-Papyrus}
      - PAPYRUS_SCANNER_DEVICE=${PAPYRUS_SCANNER_DEVICE:-airscan:w:Brother DCP-L2540DW}
      - PAPYRUS_ENCRYPTION_KEY=${PAPYRUS_ENCRYPTION_KEY}
      - PAPYRUS_SESSION_SECRET=${PAPYRUS_SESSION_SECRET}
      - PAPYRUS_BASE_URL=${PAPYRUS_BASE_URL:-http://localhost:8080}
      - PAPYRUS_DEV_MODE=${PAPYRUS_DEV_MODE:-false}
    labels:
      - traefik.enable=true
      - traefik.http.routers.papyrus.rule=Host(`${PAPYRUS_HOSTNAME:-papyrus.localhost}`)
      - traefik.http.services.papyrus.loadbalancer.server.port=8080
    depends_on:
      db:
        condition: service_healthy
    cap_add:
      - SYS_ADMIN
    restart: unless-stopped

  db:
    image: postgres:16-alpine
    ports:
      - "127.0.0.1:5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    environment:
      - POSTGRES_DB=papyrus
      - POSTGRES_USER=papyrus
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:-secret}
    healthcheck:
      test: pg_isready -U papyrus
      interval: 5s
      retries: 5
    restart: unless-stopped

volumes:
  papyrus_data:
  cups_spool:
  pgdata:
